## What and why

<!-- What changes, and what problem it solves. The "why" matters more than
     the "what" here — the diff already says what. -->

## Checks

- [ ] `pnpm test` passes (it builds first, so this covers `pnpm build` too)
- [ ] `pnpm typecheck` passes
- [ ] Fixture coverage added under `test/fixtures/` if this touches a checker
- [ ] An ADR added under `docs/adr/` if this made a real architectural tradeoff
- [ ] `README.md`'s kind table or Roadmap updated if this changes either
