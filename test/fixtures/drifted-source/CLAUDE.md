# CLAUDE.md (fixture)

A context file that has grown a paragraph since its assertions were written.
The sentence the assertion below cites used to sit at line 7; the two lines
of prose above it pushed it down to line 9, and nothing about running the
assertion itself would ever notice.

- `pnpm verify:push` runs typecheck + unit.
- `checks.yml` runs on every pull request into `main`.
